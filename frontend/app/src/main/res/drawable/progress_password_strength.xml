<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="2dp"/>
            <solid android:color="#333346"/>
        </shape>
    </item>

    <!-- Secondary progress -->
    <item android:id="@android:id/secondaryProgress">
        <scale android:scaleWidth="100%">
            <shape>
                <corners android:radius="2dp"/>
                <solid android:color="#555566"/>
            </shape>
        </scale>
    </item>

    <!-- Progress - changes color based on level -->
    <item android:id="@android:id/progress">
        <scale android:scaleWidth="100%">
            <selector>
                <!-- Weak -->
                <item android:state_checked="false">
                    <shape>
                        <corners android:radius="2dp"/>
                        <solid android:color="@color/gradient_end"/>
                    </shape>
                </item>
                <!-- Medium -->
                <item android:state_pressed="true">
                    <shape>
                        <corners android:radius="2dp"/>
                        <solid android:color="@color/darkgpurple"/>
                    </shape>
                </item>
                <!-- Strong -->
                <item android:state_selected="true">
                    <shape>
                        <corners android:radius="2dp"/>
                        <solid android:color="#6E48EB"/>
                    </shape>
                </item>
            </selector>
        </scale>
    </item>

    <!-- Glow effect -->
    <item>
        <shape>
            <gradient
                android:startColor="@color/darkgpurple"
                android:endColor="#00D1FF00"
                android:angle="270"/>
            <size android:height="2dp"/>
        </shape>
    </item>
</layer-list>